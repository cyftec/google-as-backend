{
  "name": "google-as-backend",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test:types:oauth:src": "bun x tsc --noEmit -p packages/google-oauth/tsconfig.json",
    "test:types:oauth:tests": "bun x tsc --noEmit -p packages/google-oauth/tsconfig.tests.json",
    "test:types:oauth": "bun run test:types:oauth:src && bun run test:types:oauth:tests",
    "test:types:drive-folder:src": "bun x tsc --noEmit -p packages/google-drive-folder/tsconfig.json",
    "test:types:drive-folder:tests": "bun x tsc --noEmit -p packages/google-drive-folder/tsconfig.tests.json",
    "test:types:drive-folder": "bun run test:types:drive-folder:src && bun run test:types:drive-folder:tests",
    "test:types:drive-as-socket:src": "bun x tsc --noEmit -p packages/google-drive-as-socket/tsconfig.json",
    "test:types:drive-as-socket:tests": "bun x tsc --noEmit -p packages/google-drive-as-socket/tsconfig.tests.json",
    "test:types:drive-as-socket": "bun run test:types:drive-as-socket:src && bun run test:types:drive-as-socket:tests",
    "test:types:src": "bun run test:types:oauth:src && bun run test:types:drive-folder:src && bun run test:types:drive-as-socket:src",
    "test:types:tests": "bun run test:types:oauth:tests && bun run test:types:drive-folder:tests && bun run test:types:drive-as-socket:tests",
    "test:types": "bun run test:types:src && bun run test:types:tests",
    "test:types:scripts": "bun x tsc --noEmit -p scripts/tsconfig.json",
    "test:types:monorepo": "bun x tsc --noEmit -p tsconfig.tests.json",
    "test:runtime:oauth": "bun test packages/google-oauth/tests",
    "test:runtime:drive-folder": "bun test packages/google-drive-folder/tests",
    "test:runtime:drive-as-socket": "bun test packages/google-drive-as-socket/tests",
    "test:runtime:monorepo": "bun test tests/lib.behavior.test.ts tests/publish-check.behavior.test.ts",
    "test:runtime": "bun run test:runtime:oauth && bun run test:runtime:drive-folder && bun run test:runtime:drive-as-socket && bun run test:runtime:monorepo",
    "tests": "bun run test:runtime && bun run test:types && bun run test:types:scripts && bun run test:types:monorepo",
    "publish:login": "bunx npm login",
    "publish:check": "bun scripts/publish-check.ts",
    "publish:dry-run": "bun scripts/publish.ts --dry-run",
    "publish:release": "bun scripts/publish.ts",
    "publish": "bun scripts/publish.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "bun-types": "latest",
    "typescript": "^5"
  }
}
