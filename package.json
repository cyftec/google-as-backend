{
  "name": "google-as-backend",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test:types:oauth:src": "bun x tsc --noEmit -p packages/google-oauth/tsconfig.json",
    "test:types:oauth:tests": "bun x tsc --noEmit -p packages/google-oauth/tsconfig.tests.json",
    "test:types:oauth": "bun run test:types:oauth:src && bun run test:types:oauth:tests",
    "test:types:folder:src": "bun x tsc --noEmit -p packages/google-drive-folder/tsconfig.json",
    "test:types:folder:tests": "bun x tsc --noEmit -p packages/google-drive-folder/tsconfig.tests.json",
    "test:types:folder": "bun run test:types:folder:src && bun run test:types:folder:tests",
    "test:types:socket:src": "bun x tsc --noEmit -p packages/google-drive-socket/tsconfig.json",
    "test:types:socket:tests": "bun x tsc --noEmit -p packages/google-drive-socket/tsconfig.tests.json",
    "test:types:socket": "bun run test:types:socket:src && bun run test:types:socket:tests",
    "test:types:src": "bun run test:types:oauth:src && bun run test:types:folder:src && bun run test:types:socket:src",
    "test:types:tests": "bun run test:types:oauth:tests && bun run test:types:folder:tests && bun run test:types:socket:tests",
    "test:types": "bun run test:types:src && bun run test:types:tests",
    "test:runtime:oauth": "bun test packages/google-oauth/tests",
    "test:runtime:folder": "bun test packages/google-drive-folder/tests",
    "test:runtime:socket": "bun test packages/google-drive-socket/tests",
    "test:runtime": "bun run test:runtime:oauth && bun run test:runtime:folder && bun run test:runtime:socket",
    "tests": "bun run test:runtime && bun run test:types"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "bun-types": "latest",
    "typescript": "^5"
  }
}
